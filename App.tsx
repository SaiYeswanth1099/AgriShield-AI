import { useEffect, useRef } from 'react';
import { Animated, StyleSheet, Text, View } from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import { Ionicons } from '@expo/vector-icons';
import { colors } from '../../theme';
export function SplashScreen({ onDone }: { onDone: () => void }) { const fade = useRef(new Animated.Value(0)).current; const scale = useRef(new Animated.Value(.7)).current; useEffect(() => { Animated.parallel([Animated.timing(fade, { toValue: 1, duration: 600, useNativeDriver: true }), Animated.spring(scale, { toValue: 1, useNativeDriver: true })]).start(); const id = setTimeout(onDone, 1800); return () => clearTimeout(id); }, []); return <LinearGradient colors={[colors.forest, colors.green, '#74AE42']} style={styles.page}><Animated.View style={[styles.content, { opacity: fade, transform: [{ scale }] }]}><View style={styles.mark}><Ionicons name="leaf" size={48} color={colors.forest}/></View><Text style={styles.name}>AgriShield AI</Text><Text style={styles.tagline}>AI Powered Smart Farming Platform</Text></Animated.View><View style={styles.loader}><View style={styles.dot}/><View style={[styles.dot, { opacity: .65 }]}/><View style={[styles.dot, { opacity: .3 }]}/></View></LinearGradient>; }
const styles = StyleSheet.create({ page: { flex: 1, justifyContent: 'center', alignItems: 'center' }, content: { alignItems: 'center', gap: 12 }, mark: { width: 104, height: 104, borderRadius: 36, backgroundColor: '#E8F8C7', alignItems: 'center', justifyContent: 'center' }, name: { color: '#fff', fontSize: 31, fontWeight: '900' }, tagline: { color: '#E8F8C7', fontSize: 15, fontWeight: '600' }, loader: { position: 'absolute', bottom: 72, flexDirection: 'row', gap: 7 }, dot: { height: 8, width: 8, borderRadius: 5, backgroundColor: '#fff' } });
